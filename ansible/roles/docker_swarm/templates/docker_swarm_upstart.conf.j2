description "Docker Swarm Master"
author "Matt"
console log
start on filesystem
stop on runlevel [!2345]
script
  /usr/local/bin/swarm manage --tlsverify --tlscacert=/etc/pki/tls/ca.pem --tlscert=/etc/pki/tls/swarm-cert.pem --tlskey=/etc/pki/tls/swarm-key.pem  --discovery file:///etc/swarm_config -H tcp://0.0.0.0:2376 -strategy random
end script
